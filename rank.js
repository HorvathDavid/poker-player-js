var _ = require('lodash/dist/lodash.underscore');
module.exports = {
    rankCards: function (cards) {
//        _.find(cards, function (card) {
//            return true;
//        });
        console.log(cards);
        return 0;
    }
};