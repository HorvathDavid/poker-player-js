var _ = require('lodash');
module.exports = {
    rank: function (cards) {
//        _.find(cards, function (card) {
//            return true;
//        });
        console.log(cards);
        return 0;
    }
};