var _ = require('lodash/dist/lodash.underscore');
module.exports = {
    rank: function (cards) {
//        _.find(cards, function (card) {
//            return true;
//        });
        console.log(cards);
        return 0;
    }
};